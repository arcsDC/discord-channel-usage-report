# discord-channel-usage-report

A Discord bot that generates a text report of message counts per channel over a configurable time window.

## Stack

Primary language: **TypeScript**

## Project structure

- `src/index.ts` — Analyze server message activity and output a ranked usage report.
- `package.json` — dependencies

## Usage

Review the source files and install any dependencies referenced by the project before running it.
